# Frontend Environment Configuration - Tailscale Deployment
# Use this for mobile/peer access via Tailscale network

VITE_BACKEND_URL=http://100.120.219.68:3001
VITE_SOCKET_URL=http://100.120.219.68:3001
VITE_APP_NAME=Pharmacy Scheduling System
VITE_APP_VERSION=2.0.0

# To use this configuration:
# 1. Stop the frontend service: docker compose stop frontend
# 2. Copy this file: cp frontend/.env.tailscale frontend/.env  
# 3. Restart the frontend: docker compose up -d frontend